---
import Section from "../layouts/Section.astro";
---

<section id="inicio" class="inicio">
	<div class="info">
		<strong class="title">Descubre el mundo ghibli</strong>
		<p>
			Lorem ipsum dolor sit amet consectetur adipisicing elit. Rem
			necessitatibus maiores sequi esse nemo, nam eius dignissimos vero
			delectus eum! Veritatis, deserunt. Error, id a.
		</p>
	</div>

	<div class="imagen">
		<img src="../../public/media/castle.png" alt="castillo">
	</div>
</section>

<style>
	/* INICIO */

	.inicio{		
		display: flex;
		justify-content: center;
		align-items: center;
		max-width: 100%;
		height: 100vh;
	}

	.info .title {
		font-size: var(--title);
		text-transform: uppercase;
		color: rgb(90, 161, 87);;
	}

	.info p {
		font-size: var(--text-pri);
	}

	.inicio .imagen img {
		cursor: pointer;
		transition: all .6s ease-out;
		width: 600px;
	}

	.inicio .imagen img:hover {
		cursor: pointer;
		transform: translate(20px, -20px);
	}
/* 
	@keyframes mov {
		0%{
			transform: translate(0, 0);
		}
		30%{
			
		}
		40%{
			transform: translate(15px, -5px);
		}
		50%{
			transform: translate(20px, -15px);
		}
		60%{
			transform: translate(15px, -5px);
		}
		50%{
			transform: translate(20px, -15px);
		}
		60%{
			transform: translate(0, 0);
		}
	} */
</style>
